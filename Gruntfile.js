'use strict';

module.exports = function (grunt) {

    grunt.initConfig({



    });

    /**
     * Grunt Task Modules
     *
     * Grunt modules used by the
     * tasks
     *
     * npm install {module_name}
     */
    grunt.loadNpmTasks('');


    /**
     * Grunt Task Groups
     *
     * Task groups to run multiple
     * tasks in one go
     */
    // grunt.registerTask('', []);

};
